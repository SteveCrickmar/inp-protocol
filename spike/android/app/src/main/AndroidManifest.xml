<?xml version="1.0" encoding="utf-8"?>
<!-- DISPOSABLE Phase-0 spike (OC-5). INP S0.5 Android probe. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!--
          android:enableOnBackInvokedCallback="true" opts this app into the
          predictive-back system back animation on Android 13+ (and the
          predictive cross-activity / system animations that became user-visible
          on API 34+). This is the flag the S0.5 predictive-back observation in
          MainActivity depends on; without it OnBackInvokedCallback behaviour and
          the predictive UI differ — record that delta in ADR-0003.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
